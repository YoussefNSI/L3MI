// Calcul du tableau des noms des convoqués, sans doublons, et trié par ordre alphabétique, par lecture du tableau HTML "CONVOQUES"
var convoques = [];
function extraireConvoques() {
	convoques = [];
	// A COMPLETER
}
extraireConvoques();


// Calcul du tableau des noms des exempts, trié par ordre alphabétique, par lecture du tableau HTML "EXEMPTS"
var exempts = [];
function extraireExempts() {
	exempts = [];
	// A COMPLETER
}
extraireExempts();


// Calcul du tableau des noms des personnes convoquables, trié par ordre alphabétique,
// par "union" des tableaux convoques et exempts
var convoquables = [];
function calculerConvoquables() {
	convoquables = [];
	// A COMPLETER
}
calculerConvoquables();

// Masquer les images des exempts, afficher les images des convoques
function gererImages() {
	// A COMPLETER
}

gererImages();

// Mise à jour du tableau exempts à partir des tableaux convoquables et convoques
function mettreAJourExempts() {
	exempts = [];
	// A COMPLETER
}

// Mise à jour du tableau HTML EXEMPTS à partir du tableau exempts
function regenererTableExempts() {
	// A COMPLETER
}

// Gestion de modification de convocation
// A COMPLETER
